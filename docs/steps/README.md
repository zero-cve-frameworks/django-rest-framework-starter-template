# Steps

## Project setup

[Source](https://www.django-rest-framework.org/tutorial/quickstart/#project-setup)

```bash
pip install djangorestframework

django-admin startproject core .

python manage.py startapp scrapers
```

## Move the `scrapers` app to `apps` directory and configure `settings.py`.
